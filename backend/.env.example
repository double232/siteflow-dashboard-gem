# SiteFlow Dashboard Backend Configuration

# Hetzner Server SSH Connection
HETZNER_HOST=192.0.2.123
HETZNER_USER=root
HETZNER_PORT=22
HETZNER_KEY_PATH=C:\\Keys\\hetzner_ed25519
SSH_TIMEOUT=30

# SSH Security - Path to known_hosts file for host key verification
# Generate with: ssh-keyscan -H your-server-ip >> ~/.ssh/known_hosts
SSH_KNOWN_HOSTS=

# Remote Paths
REMOTE_SITES_ROOT=/opt/sites
REMOTE_GATEWAY_ROOT=/opt/gateway
REMOTE_CADDYFILE=/opt/gateway/Caddyfile
REMOTE_DATA_ROOT=/opt/data

# Cloudflare (optional)
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_TUNNEL_ID=

# Caching
CACHE_TTL_SECONDS=20
LOG_SSH_COMMANDS=true

# Database
SQLITE_DB_PATH=siteflow.db
AUDIT_RETENTION_DAYS=90
AUDIT_MAX_OUTPUT_LENGTH=10000

# NAS Backup Monitoring (optional)
NAS_HOST=
NAS_USER=
NAS_PASSWORD=
NAS_SHARE=volume1
NAS_BACKUP_PATH=backups
NAS_STALE_THRESHOLD_HOURS=48
NAS_POLL_INTERVAL_SECONDS=300

# Uptime Kuma
KUMA_URL=http://uptime-kuma:3001
KUMA_USERNAME=admin
KUMA_PASSWORD=

# WebSocket
WS_MONITOR_INTERVAL=10.0

# CORS - Comma-separated list of allowed origins
# In production, set to your dashboard domain
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# OAuth2 Proxy Authentication (Production)
# ===========================================
# These are used with docker-compose.auth.yml
# Create credentials at: https://console.cloud.google.com/apis/credentials

# Google OAuth2 credentials
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=

# Random 32-byte secret for cookie encryption
# Generate with: openssl rand -base64 32
OAUTH2_COOKIE_SECRET=

# Your dashboard domain (without https://)
DASHBOARD_DOMAIN=dashboard.example.com
